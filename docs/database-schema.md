# Database schema

Реализованы домены: communities, profiles, community_contacts, events, event_registrations, user_settings, synced_contacts, calendar_cache.

Для пользовательских таблиц включён RLS с политикой доступа только к собственным данным.
