# Test-only overrides layered after api.env.example by api_test_backend.
# These values are synthetic and must never be reused for local working data.
APP_ENV=test
DATABASE_URL=postgresql+asyncpg://sredi_api_test:sredi_api_test@api_test_postgres:5432/sredi_api_test
